version: '3.8'
services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3333:3000"
    volumes:
      - grafana_vol:/var/lib/grafana
    restart: unless-stopped

volumes:
  grafana_vol:
